namespace Маяки.GameWindow
{
    /// <summary>
    /// Enum, отвечающий за значения состояния клетки
    /// </summary>
    public enum CellValueEnum
    {
        None,
        Boat,
        Blocked,
        Lighthouse,
        Unavailable
    }
}
